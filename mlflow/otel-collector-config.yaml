# DEPRECATED: This file is superseded by ../otel-collector/otel-collector.yaml
#
# The canonical OTel collector configuration lives in otel-collector/otel-collector.yaml
# and includes critical processors missing from this copy:
#   - filter/drop-probes (without this, probe spans outnumber real traces 150:1)
#   - MLflow spanType OTTL transform (populates the Span Type column)
#   - Kagent agent peer.service transforms (Jaeger dependency graph edges)
#   - Non-deprecated exporter aliases (otlp_http, otlp_grpc/*)
#
# Do not apply this file directly. Use otel-collector/otel-collector.yaml instead.
